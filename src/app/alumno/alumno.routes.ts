import { Routes } from "@angular/router";
import AlumnoVistaComponent from "./alumno-vista/alumno-vista.component";

const alumnoRoutes: Routes = [
    {
        path: '',
        component: AlumnoVistaComponent
    }
];

export default alumnoRoutes;
